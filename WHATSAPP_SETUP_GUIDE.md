# دليل ربط واتساب والإرسال التلقائي — خطوة بخطوة

> كل عيادة لها إعدادات واتساب مستقلة ضمن حسابها: رقم العيادة، وضع الإرسال، القالب، وسجل الرسائل.

## الوضع الأول: العادي wa.me (سهل ومجاني) ✅ موصى به للبدء

### الخطوات:
1. سجّل الدخول بحساب عيادتك ثم اضغط 📱 ربط واتساب.
2. اختر "الوضع العادي - wa.me".
3. أدخل رقم واتساب العيادة (مثال: 0791234567).
4. فعّل "تفعيل تلقائي" ✅.
5. اضغط "🔗 ربط واتساب ومنح الصلاحية".
6. لكل مادة تريد تتبعها: من 📦 المواد فعّل «واتساب تلقائي» ✅ وتأكد من ربطها بموردها المعتمد وحد التنبيه.

### كيف يعمل الإرسال التلقائي هنا؟
- عند وصول أي مادة مفعّلة للحد الأدنى (من أي شاشة)، يظهر **تنبيه فوري** بزر «💬 فتح واتساب وإرسال».
- ضغطة واحدة تفتح محادثة المورد المعتمد برسالة جاهزة (اسم العيادة + المواد + الكميات تلقائياً).
- ملاحظة تقنية: المتصفح يمنع فتح نوافذ واتساب بدون ضغطة مستخدم، لذلك wa.me يحتاج هذه الضغطة الواحدة فقط — أما Cloud API فيرسل بدون أي تدخل.

## الوضع الثاني: Cloud API التلقائي الكامل 🤖 بدون أي تدخل

### المتطلبات:
- حساب Facebook Business Manager.
- رقم هاتف لم يستخدم في واتساب عادي (أو تحويله لبيزنس).
- بطاقة دفع (Meta تطلبها لكن أول 1000 محادثة شهرياً مجانية).

### الخطوات التفصيلية:

#### 1. إنشاء تطبيق في Meta:
- اذهب https://developers.facebook.com -> My Apps -> Create App
- اختر Business -> اسم التطبيق "Dental Clinic Inventory"
- أضف منتج WhatsApp

#### 2. إعداد WhatsApp:
- من القائمة الجانبية: WhatsApp -> API Setup
- انسخ Phone Number ID و Access Token.

#### 3. في برنامج الجرد (داخل حساب عيادتك):
- افتح 📱 ربط واتساب.
- اختر "API تلقائي كامل - Cloud API".
- الصق Phone Number ID و Access Token.
- أدخل رقم العيادة.
- فعّل "تفعيل تلقائي" ✅.
- **للإرسال بدون أي تدخل**: عطّل "تأكيد قبل الإرسال" ❌ (اتركه مفعّلاً إذا أردت زر «إرسال الآن» للتأكيد).
- اضبط «إعادة التنبيه لنفس المادة» (افتراضياً كل 24 ساعة).
- اضغط "🧪 اختبار API" للتأكد، ثم "💾 حفظ".

#### 4. الإرسال التلقائي:
- محرك المراقبة يفحص **كل تغيير** في الكميات: أي مادة مفعّل لها «واتساب تلقائي» ووصلت حدها الأدنى → رسالة فورية للمورد المعتمد عبر API.
- لن تتكرر نفس الرسالة قبل مدة «إعادة التنبيه» إلا إذا تغيرت الكمية.
- كل رسالة (ناجحة/فاشلة) تُسجل في **📜 سجل الإرسال التلقائي** داخل نفس الشاشة.

### حل مشكلة CORS:
- عند التشغيل عبر الخادم (`npm start`) يتم الإرسال عبر **وسيط داخلي** `/api/send-whatsapp` — بدون أي مشاكل CORS وبدون إعدادات إضافية.
- عند الفتح المباشر بدون خادم، يحاول البرنامج الإرسال المباشر (قد يمنعه المتصفح) — الحل: شغّل `npm start`.

### التكلفة:
- أول 1000 محادثة شهرياً مجانية.
- بعدها حسب تسعيرة Meta (حوالي $0.02-0.05 لكل محادثة).

## قالب الرسالة
القالب الافتراضي يدعم المتغيرات التالية (تُستبدل تلقائياً):
- `{repName}` اسم المندوب، `{companyName}` اسم شركة المورد.
- `{itemsList}` قائمة المواد الناقصة مع الكميات والأماكن.
- `{date}` تاريخ اليوم، `{clinicName}` اسم عيادتك (من حسابك).

## أي وضع أختار؟
- **عيادة صغيرة/متوسطة**: استخدم الوضع العادي wa.me — تنبيه فوري + ضغطة واحدة.
- **عيادة تريد أتمتة كاملة**: استخدم Cloud API مع تعطيل «تأكيد قبل الإرسال».

يمكنك التبديل بين الوضعين في أي وقت من إعدادات واتساب داخل حساب عيادتك.
